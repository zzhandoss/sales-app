import { describe, expect, it } from 'vitest';

describe('order cancel contract', () => {
  it('supports cancel endpoint for immutable submitted orders', () => {
    expect(true).toBe(true);
  });
});
